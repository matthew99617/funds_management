import 'package:flutter/material.dart';

class AddForwardPlans extends StatefulWidget {
  const AddForwardPlans({Key? key}) : super(key: key);

  @override
  State<AddForwardPlans> createState() => _AddForwardPlansState();
}

class _AddForwardPlansState extends State<AddForwardPlans> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
